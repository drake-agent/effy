# ═══════════════════════════════════════════════════════════════
# Effy — Production Environment Overrides
# base config(effy.config.yaml)와 다른 값만 명시.
# Usage: NODE_ENV=production
# ═══════════════════════════════════════════════════════════════

# production은 base config과 동일 → 오버라이드 최소화
# 필요 시 여기에 production 전용 값만 추가

logging:
  level: warn
